# CLAUDE.md — Agent Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

## Project Purpose

Cloudflare Pages app that scores and rewrites prompts with a 7-criterion rubric. Frontend in `dist/`, backend in `functions/`. Talks to AI providers; keys must stay in Cloudflare/env secrets, never in git.

**Repository:** `ai-alchemy-prompt-evaluator`
**Risk Level:** HIGH

## Allowed Operations

- Read `README.md`, `app.py`, `functions/`, and `prompt_knowledge_base.md`.
- Edit rubric text or UI copy in small diffs.

## Forbidden Operations

- Do **not** delete files without explicit user confirmation.
- Do **not** commit or push changes automatically.
- Do **not** read, print, or log the contents of `.env`, secrets, or credential files.
- Do **not** install packages outside a virtual environment without asking.
- Do **not** run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`, etc.).
- Do **not** deploy from an agent session.
- Do **not** put API keys in `functions/` or `dist/`.
- Do not read Cloudflare secret values.

## Blocked Paths (never read contents)

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Environment

- **Language(s):** Python, JavaScript
- **Build / install:** requirements.txt plus Cloudflare Pages functions

## In-scope edit areas

Prefer small edits in existing project folders. Stay out of `.git/`, virtualenvs, and secret files.

## Notes

- Live deploy is connected to `main` — every push can go public. Prefer a branch.
- HIGH classification is from multi-provider/API-key *patterns*, not a committed `.env` file.

## How to verify

```bash
echo 'Review functions/ for hardcoded secrets (filenames and assignment names only).'
```

Always run the verification command before claiming a task is complete.
